{
  "name": "client",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "rm -rf dist && vue-tsc -p ./tsconfig.json && vite build",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "vue": "~3.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5",
    "vite": "^5",
    "vue-tsc": "~1.8"
  }
}
